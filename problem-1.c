#include <stdio.h>

int main() {
    char name[100];
    
    // Read the input string
    scanf("%s", name);
    
    // Print the greeting message
    printf("Hello, %s\n", name);
    
    return 0;
}